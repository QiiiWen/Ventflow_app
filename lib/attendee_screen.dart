import 'package:flutter/material.dart';

class AttendeeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Attendee Dashboard")),
      body: Center(child: Text("Welcome, Attendee!")),
    );
  }
}
