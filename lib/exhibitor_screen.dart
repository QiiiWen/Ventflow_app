import 'package:flutter/material.dart';

class ExhibitorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Exhibitor Dashboard")),
      body: Center(child: Text("Welcome, Exhibitor!")),
    );
  }
}
